import { Box } from "@/components/ui/box";
import React from "react";
import { Text } from "react-native";

const Tickets = () => {
  return (
    <Box className="flex-1 bg-primary-950">
      <Text>Community Stuff</Text>
    </Box>
  );
};

export default Tickets;
